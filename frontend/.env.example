# URL of the authoritative realtime server (Socket.IO).
# Local dev usually points at the Node server started with `npm run dev` in ../server.
# For deployed frontend hosts like Vercel or Hugging Face, set this to the deployed
# backend URL (for example your Render instance), not localhost.
VITE_WS_URL=http://localhost:8787
